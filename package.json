{
  "name": "shell-protocol-pool",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ voronsv/shell-protocol-pool",
    "create-local": "graph create --node http://localhost:8020/ voronsv/shell-protocol-pool",
    "remove-local": "graph remove --node http://localhost:8020/ voronsv/shell-protocol-pool",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 voronsv/shell-protocol-pool"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.19.0",
    "@graphprotocol/graph-ts": "0.19.0"
  }
}
